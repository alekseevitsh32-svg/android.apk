name: Build Android APK
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Prepare WWW Folder
        run: |
          mkdir -p www
          cp index.html www/
          cp style.css www/
          cp app.js www/
          cp privacy.html www/

      - name: Install Capacitor
        run: |
          npm install @capacitor/core @capacitor/cli
          npm install @capacitor/android
          npx cap init "Выгодный Шоппинг" "ru.dima.smartshopping" --web-dir=www

      - name: Add Android Platform
        run: npx cap add android

      - name: Generate App Icons
        run: |
          npm install @capacitor/assets
          npx capacitor-assets generate --icon
        continue-on-error: true

      - name: Sync Code to Android
        run: npx cap sync android

      # 🔥 НАШ ХАК: Заставляем весь проект использовать стабильную версию библиотеки 1.70 вместо глючной 1.79
      - name: Force Safe Library Version in Android Project
        run: |
          echo "configurations.all { resolutionStrategy { force 'org.bouncycastle:bcprov-jdk18on:1.70' } }" >> android/build.gradle

      - name: Build Android Release APK
        run: |
          cd android && ./gradlew assembleRelease

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-release
          path: android/app/build/outputs/apk/release/app-release-unsigned.apk
